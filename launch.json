{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start script",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/allvideouser.js"
        }
    ]
}